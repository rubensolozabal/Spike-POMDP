from setuptools import setup, find_packages

setup(
    name='pomdp-baselines',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # list your package's dependencies here, e.g.,
        # 'numpy>=1.18.0',
    ],
    # Add all package configuration here
)